[package]
name = "plato-health"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Health check system for PLATO rooms — uptime, response times, error rates"
repository = "https://github.com/SuperInstance/plato-health"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "serde"] }

[dev-dependencies]
serde_json = "1"
